<html>
<head>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
    <title>Cars!!</title>
</head>
<body>
  <h1>Find YOUR new car...</h1>
  {% if cars is not empty %}
    <p>Find your new car:</p>
    <ul>
      {% for cars in car %}
        <li>Type:{{ car.getMake }}</li>
        <li><img src= {{ car.getImage }}></li>
        <li>Miles: {{ car.getMiles }}</li>
        <li>Price: ${{ car.getPrice }}</li>
      {% end for %}
    </ul>
  {% endif %}

    <form action='/buyCar'>
      <div class='form-group'>
        <label for='price'>Enter your max price: $</label>
        <input id='price' name='price' class='form-control' type='number'>
      </div>
      <div class='form-group'>
        <label for='miles'>Enter your desired mileage</label>
        <input id='miles' name='miles' class='form-control' type='number'>
      </div>
      <button type='submit' class='btn-danger'>Search now!</button>
    </form>
    <div class='/sell'>
       <h2>Interested in selling your car? click here for directions on how to sell today!</h2>
       <button type="submit" class='btn-danger'>Sell now!</button>
    </div>
  </div>
</body>
</html>
